name: customer_revenue_attribution
version: "1.0.0"
config-version: 2

profile: customer_revenue_attribution

model-paths: ["models"]
test-paths: ["tests"]
clean-targets:
  - target
  - dbt_packages

models:
  customer_revenue_attribution:
    staging:
      +materialized: table
    intermediate:
      +materialized: table
      +schema: intermediate
